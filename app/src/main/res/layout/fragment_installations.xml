<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Instalaciones"
                android:textStyle="bold"
                android:textSize="20sp"
                android:padding="16dp"/>

            <!-- Fechas (fecha exacta + rango) -->
            <!-- (Luego lo dejamos bonito con TextInputLayout y DatePicker) -->

            <include
                android:id="@+id/includeSummary"
                layout="@layout/view_installations_summary"/>

            <!-- Buscador + filtro -->
            <!-- RecyclerView de cards verdes (con headers por fecha) -->

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>